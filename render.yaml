services:
  - type: web
    name: abarrotes-flor
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn ABARROTES:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
