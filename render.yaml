services:
  - type: web
    name: abarrotes-flor
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -b 0.0.0.0:$PORT ABARROTES:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
